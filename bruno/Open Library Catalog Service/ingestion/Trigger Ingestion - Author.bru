meta {
  name: Trigger Ingestion - Author
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/ingest
  body: json
  auth: none
}

headers {
  X-API-Key: {{api_key}}
}

body:json {
  {
    "query_type": "author",
    "query_value": "tolkien"
  }
}

script:post-response {
  if (res.status === 202) {
    bru.setEnvVar("log_id", res.body.log_id);
  }
}
