meta {
  name: Create Tenant
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/admin/tenants
  body: json
  auth: none
}

body:json {
  {
    "name": "central-library"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setEnvVar("api_key", res.body.api_key);
  }
}
